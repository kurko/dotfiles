- There's the ISP router with its own network, and my own
  private network (DHCP) plugged to that. From my private
  network router, there's one hop to the ISP-provided
  router/modem/wifi antenna, which I don't use directly
  other than to receive internet (fiber cable).
- Office computer is plugged via ethernet to my Deco/router.
  If wifi is being used as default, please flag.
- I generally use 1.1.1.1 as DNS server.
- Deco for wifi mesh.
- We should likely monitor for more than a few seconds
  because most times packets are lost intermittenly.
